/**
 * Write filtering code here. The IGV will provide the following global symbols:
 * - graph, a Diagram object, which you can use to access and modify graph representation
 * You can use JS helper functions:
 * - colorize(property, value, color) -  to mark nodes of certain category with a color
 * - remove(property, value) -  to remove matching nodes from graph
 * See the documentation for additional helpers and more details.
 */

