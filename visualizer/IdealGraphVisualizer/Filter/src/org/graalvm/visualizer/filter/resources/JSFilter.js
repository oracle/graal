/*
 * The function should modify the visual representation (the diagram). It may
 * read directly the underlying data from the InputGraph.
 * 
 * @param {Diagram} the input data - the InputGraph. 
 * @param {PrintStream} output stream
 */
function process(graph, IO) {
    
}

// identifies the main filter function. DO NOT MODIFY
// IGV-entry-point: process(graph, IO);
