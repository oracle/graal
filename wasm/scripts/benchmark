#!/usr/bin/env bash

SUITE=$1
BENCHMARK=$2

if [ $# -lt 2 ]; then
    echo "usage: benchmark [suite] [benchmark]"
    exit 1
fi

mx -v --dy /compiler benchmark wasm:WASM_BENCHMARKCASES -- -Dwasmbench.benchmarkName=$BENCHMARK -- $SUITE
